[project]
name = "aieos-ai-gateway"
version = "0.0.0"
description = "AIEOS ai gateway bootstrap package."
requires-python = "==3.13.*"
dependencies = []

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aieos/ai_gateway"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
